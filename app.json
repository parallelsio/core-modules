{
  "name": "core-modules",
  "description": "",
  "scripts": {},
  "env": {
    "GRAPHENEDB_URL": {
      "required": true,
      "description": "Neo4j DB connection URI"
    },
    "MONGOLAB_URI": {
      "required": true,
      "description": "Mongolab DB connection URI"
    },
    "MONGO_URL": {
      "required": true,
      "description": "Meteor only looks for MONGO_URL to connect to a Mongo DB"
    },
    "UPLOADER": {
      "required": true,
      "description": "Type of uploader to use for multimedia upload: (s3Uploader)"
    },
    "AWS_ACCESS_KEY_ID": {
      "required": true,
      "description": "AWS_ACCESS_KEY_ID"
    },
    "AWS_BUCKET": {
      "required": true,
      "description": "AWS_BUCKET"
    },
    "AWS_SECRET_ACCESS_KEY": {
      "required": true,
      "description": "AWS_SECRET_ACCESS_KEY"
    },
    "ROOT_URL": {
      "require": true,
      "description": "URL of the hosted Heroku app: (makeparallels-pr-[PULL_REQUEST_NUMBER])"
    }
  },
  "addons": [],
  "buildpacks": [
    {
      "url": "https://github.com/AngelCabo/meteor-buildpack-horse.git"
    }
  ]
}